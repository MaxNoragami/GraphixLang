{
    BATCH #mybatch = "images/";
    FOREACH IMG $img IN #mybatch EXPORT TO "C:/processed_photos/" {
        SET $img SEPIA;
        ROTATE $img LEFT;
        CROP $img (100, 200);
        INT $width = METADATA $img FWIDTH;
    }
}